@AGENTS.md

## Claude Code

- Skills in `.claude/skills/` are shared with Cursor — keep frontmatter to `name` and `description` only.
- Hooks in `.claude/settings.json` guard commands and writes; a denial names the fix.
