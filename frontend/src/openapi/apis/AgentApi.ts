/* tslint:disable */
/* eslint-disable */
/**
 * Overbae API
 * Backend API for the Overclaw / Overmind platform.
 *
 * The version of the OpenAPI document: 2.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  AgentGraph,
} from '../models/index';
import {
    AgentGraphFromJSON,
    AgentGraphToJSON,
} from '../models/index';

export interface AgentRetrieveRequest {
    project: string;
}

/**
 *
 */
export class AgentApi extends runtime.BaseAPI {

    /**
     * One request draws the agent page: current capabilities, evidence edges weighted by observed runtime transitions, and the latest scan.
     * Get the project\'s agent graph
     */
    async agentRetrieveRaw(requestParameters: AgentRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<AgentGraph>> {
        if (requestParameters['project'] == null) {
            throw new runtime.RequiredError(
                'project',
                'Required parameter "project" was null or undefined when calling agentRetrieve().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/agent/`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => AgentGraphFromJSON(jsonValue));
    }

    /**
     * One request draws the agent page: current capabilities, evidence edges weighted by observed runtime transitions, and the latest scan.
     * Get the project\'s agent graph
     */
    async agentRetrieve(requestParameters: AgentRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<AgentGraph> {
        const response = await this.agentRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

}
