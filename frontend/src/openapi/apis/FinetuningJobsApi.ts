/* tslint:disable */
/* eslint-disable */
/**
 * Overbae API
 * Backend API for the Overclaw / Overmind platform.
 *
 * The version of the OpenAPI document: 2.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  DatasetOverlapResponse,
  DatasetValidateRequestRequest,
  DatasetValidationResponse,
  EvalRunsDatasetsRetrieve200ResponseInner,
  FinetuningEstimateRequestRequest,
  FinetuningEstimateResponse,
  FinetuningExperiment,
  FinetuningJob,
  FinetuningJobRequest,
  FinetuningModelCatalogResponse,
  FinetuningModelDefaultsRequestRequest,
  FinetuningRecommendRequestRequest,
  FinetuningRecommendationResponse,
  ModelSwapPrompt,
  PaginatedFinetuningJobEventList,
  PaginatedFinetuningJobListList,
  PaginatedFinetuningJobRunList,
  PatchedFinetuningJobRequest,
} from '../models/index';
import {
    DatasetOverlapResponseFromJSON,
    DatasetOverlapResponseToJSON,
    DatasetValidateRequestRequestFromJSON,
    DatasetValidateRequestRequestToJSON,
    DatasetValidationResponseFromJSON,
    DatasetValidationResponseToJSON,
    EvalRunsDatasetsRetrieve200ResponseInnerFromJSON,
    EvalRunsDatasetsRetrieve200ResponseInnerToJSON,
    FinetuningEstimateRequestRequestFromJSON,
    FinetuningEstimateRequestRequestToJSON,
    FinetuningEstimateResponseFromJSON,
    FinetuningEstimateResponseToJSON,
    FinetuningExperimentFromJSON,
    FinetuningExperimentToJSON,
    FinetuningJobFromJSON,
    FinetuningJobToJSON,
    FinetuningJobRequestFromJSON,
    FinetuningJobRequestToJSON,
    FinetuningModelCatalogResponseFromJSON,
    FinetuningModelCatalogResponseToJSON,
    FinetuningModelDefaultsRequestRequestFromJSON,
    FinetuningModelDefaultsRequestRequestToJSON,
    FinetuningRecommendRequestRequestFromJSON,
    FinetuningRecommendRequestRequestToJSON,
    FinetuningRecommendationResponseFromJSON,
    FinetuningRecommendationResponseToJSON,
    ModelSwapPromptFromJSON,
    ModelSwapPromptToJSON,
    PaginatedFinetuningJobEventListFromJSON,
    PaginatedFinetuningJobEventListToJSON,
    PaginatedFinetuningJobListListFromJSON,
    PaginatedFinetuningJobListListToJSON,
    PaginatedFinetuningJobRunListFromJSON,
    PaginatedFinetuningJobRunListToJSON,
    PatchedFinetuningJobRequestFromJSON,
    PatchedFinetuningJobRequestToJSON,
} from '../models/index';

export interface FinetuningJobsBaseModelsRetrieveRequest {
    baseModel?: string;
    capability?: string;
    dataset?: string;
    project?: string;
    search?: string;
    status?: string;
}

export interface FinetuningJobsCancelCreateRequest {
    id: string;
}

export interface FinetuningJobsCreateRequest {
    finetuningJobRequest: FinetuningJobRequest;
}

export interface FinetuningJobsDatasetOverlapRetrieveRequest {
    dataset?: string;
    evalDataset?: string;
}

export interface FinetuningJobsDatasetsRetrieveRequest {
    baseModel?: string;
    capability?: string;
    dataset?: string;
    project?: string;
    search?: string;
    status?: string;
}

export interface FinetuningJobsDestroyRequest {
    id: string;
}

export interface FinetuningJobsEstimateCreateRequest {
    finetuningEstimateRequestRequest: FinetuningEstimateRequestRequest;
}

export interface FinetuningJobsEventsListRequest {
    id: string;
    baseModel?: string;
    capability?: string;
    createdAfter?: Date;
    createdBefore?: Date;
    dataset?: string;
    groupId?: string;
    ordering?: string;
    page?: number;
    pageSize?: number;
    project?: string;
    provider?: FinetuningJobsEventsListProviderEnum;
    search?: string;
    status?: FinetuningJobsEventsListStatusEnum;
}

export interface FinetuningJobsListRequest {
    baseModel?: string;
    capability?: string;
    createdAfter?: Date;
    createdBefore?: Date;
    dataset?: string;
    groupId?: string;
    ordering?: string;
    page?: number;
    pageSize?: number;
    project?: string;
    provider?: FinetuningJobsListProviderEnum;
    search?: string;
    status?: FinetuningJobsListStatusEnum;
}

export interface FinetuningJobsLossCurvesRetrieveRequest {
    id: string;
}

export interface FinetuningJobsModelDefaultsCreateRequest {
    finetuningModelDefaultsRequestRequest: FinetuningModelDefaultsRequestRequest;
}

export interface FinetuningJobsModelSwapPromptRetrieveRequest {
    id: string;
    pin?: boolean;
}

export interface FinetuningJobsModelsRetrieveRequest {
    hasToolCalling?: boolean;
    maxContext?: number;
}

export interface FinetuningJobsPartialUpdateRequest {
    id: string;
    patchedFinetuningJobRequest?: PatchedFinetuningJobRequest;
}

export interface FinetuningJobsRecommendCreateRequest {
    finetuningRecommendRequestRequest: FinetuningRecommendRequestRequest;
}

export interface FinetuningJobsRetrieveRequest {
    id: string;
}

export interface FinetuningJobsRetryCreateRequest {
    id: string;
}

export interface FinetuningJobsRunsListRequest {
    baseModel?: string;
    capability?: string;
    createdAfter?: Date;
    createdBefore?: Date;
    dataset?: string;
    groupId?: string;
    ordering?: string;
    page?: number;
    pageSize?: number;
    project?: string;
    provider?: FinetuningJobsRunsListProviderEnum;
    search?: string;
    status?: FinetuningJobsRunsListStatusEnum;
}

export interface FinetuningJobsValidateDatasetCreateRequest {
    datasetValidateRequestRequest: DatasetValidateRequestRequest;
}

/**
 *
 */
export class FinetuningJobsApi extends runtime.BaseAPI {

    /**
     * Option list for the training-history model filter, read off the jobs for the same reason as the dataset facet: an option that yields no rows — or a row whose value is missing from the options — is a bug.
     * List distinct base models used by finetuning jobs visible to the caller
     */
    async finetuningJobsBaseModelsRetrieveRaw(requestParameters: FinetuningJobsBaseModelsRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<string>>> {
        const queryParameters: any = {};

        if (requestParameters['baseModel'] != null) {
            queryParameters['base_model'] = requestParameters['baseModel'];
        }

        if (requestParameters['capability'] != null) {
            queryParameters['capability'] = requestParameters['capability'];
        }

        if (requestParameters['dataset'] != null) {
            queryParameters['dataset'] = requestParameters['dataset'];
        }

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/base-models/`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Option list for the training-history model filter, read off the jobs for the same reason as the dataset facet: an option that yields no rows — or a row whose value is missing from the options — is a bug.
     * List distinct base models used by finetuning jobs visible to the caller
     */
    async finetuningJobsBaseModelsRetrieve(requestParameters: FinetuningJobsBaseModelsRetrieveRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<string>> {
        const response = await this.finetuningJobsBaseModelsRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Cancel a finetuning job
     */
    async finetuningJobsCancelCreateRaw(requestParameters: FinetuningJobsCancelCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningJob>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling finetuningJobsCancelCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/{id}/cancel/`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningJobFromJSON(jsonValue));
    }

    /**
     * Cancel a finetuning job
     */
    async finetuningJobsCancelCreate(requestParameters: FinetuningJobsCancelCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningJob> {
        const response = await this.finetuningJobsCancelCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Schedule a finetuning job
     */
    async finetuningJobsCreateRaw(requestParameters: FinetuningJobsCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningJob>> {
        if (requestParameters['finetuningJobRequest'] == null) {
            throw new runtime.RequiredError(
                'finetuningJobRequest',
                'Required parameter "finetuningJobRequest" was null or undefined when calling finetuningJobsCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FinetuningJobRequestToJSON(requestParameters['finetuningJobRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningJobFromJSON(jsonValue));
    }

    /**
     * Schedule a finetuning job
     */
    async finetuningJobsCreate(requestParameters: FinetuningJobsCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningJob> {
        const response = await this.finetuningJobsCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Number of training datapoints whose source trace also appears in the chosen eval dataset. These rows are excluded from training when the job links that eval dataset.
     * Count train/eval overlapping datapoints
     */
    async finetuningJobsDatasetOverlapRetrieveRaw(requestParameters: FinetuningJobsDatasetOverlapRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DatasetOverlapResponse>> {
        const queryParameters: any = {};

        if (requestParameters['dataset'] != null) {
            queryParameters['dataset'] = requestParameters['dataset'];
        }

        if (requestParameters['evalDataset'] != null) {
            queryParameters['eval_dataset'] = requestParameters['evalDataset'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/dataset-overlap/`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DatasetOverlapResponseFromJSON(jsonValue));
    }

    /**
     * Number of training datapoints whose source trace also appears in the chosen eval dataset. These rows are excluded from training when the job links that eval dataset.
     * Count train/eval overlapping datapoints
     */
    async finetuningJobsDatasetOverlapRetrieve(requestParameters: FinetuningJobsDatasetOverlapRetrieveRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DatasetOverlapResponse> {
        const response = await this.finetuningJobsDatasetOverlapRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Option list for the training-history dataset filter: one entry per dataset that has at least one job. Read off the *jobs*, not the project\'s dataset list, so the option list and the `?dataset=` filter can never disagree about which values yield rows — deriving options from the loaded page instead both misses legitimate values and hides the control entirely once the first page happens to be uniform.
     * List distinct datasets used by finetuning jobs visible to the caller
     */
    async finetuningJobsDatasetsRetrieveRaw(requestParameters: FinetuningJobsDatasetsRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<Array<EvalRunsDatasetsRetrieve200ResponseInner>>> {
        const queryParameters: any = {};

        if (requestParameters['baseModel'] != null) {
            queryParameters['base_model'] = requestParameters['baseModel'];
        }

        if (requestParameters['capability'] != null) {
            queryParameters['capability'] = requestParameters['capability'];
        }

        if (requestParameters['dataset'] != null) {
            queryParameters['dataset'] = requestParameters['dataset'];
        }

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/datasets/`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => jsonValue.map(EvalRunsDatasetsRetrieve200ResponseInnerFromJSON));
    }

    /**
     * Option list for the training-history dataset filter: one entry per dataset that has at least one job. Read off the *jobs*, not the project\'s dataset list, so the option list and the `?dataset=` filter can never disagree about which values yield rows — deriving options from the loaded page instead both misses legitimate values and hides the control entirely once the first page happens to be uniform.
     * List distinct datasets used by finetuning jobs visible to the caller
     */
    async finetuningJobsDatasetsRetrieve(requestParameters: FinetuningJobsDatasetsRetrieveRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<Array<EvalRunsDatasetsRetrieve200ResponseInner>> {
        const response = await this.finetuningJobsDatasetsRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Delete finetuning job
     */
    async finetuningJobsDestroyRaw(requestParameters: FinetuningJobsDestroyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling finetuningJobsDestroy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/{id}/`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     * Delete finetuning job
     */
    async finetuningJobsDestroy(requestParameters: FinetuningJobsDestroyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.finetuningJobsDestroyRaw(requestParameters, initOverrides);
    }

    /**
     * Uses the dataset\'s real token stats and published provider pricing to estimate cost/time for an edited n_epochs / LoRA vs full setting. Does not create a job.
     * Re-estimate fine-tuning cost and duration
     */
    async finetuningJobsEstimateCreateRaw(requestParameters: FinetuningJobsEstimateCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningEstimateResponse>> {
        if (requestParameters['finetuningEstimateRequestRequest'] == null) {
            throw new runtime.RequiredError(
                'finetuningEstimateRequestRequest',
                'Required parameter "finetuningEstimateRequestRequest" was null or undefined when calling finetuningJobsEstimateCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/estimate/`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FinetuningEstimateRequestRequestToJSON(requestParameters['finetuningEstimateRequestRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningEstimateResponseFromJSON(jsonValue));
    }

    /**
     * Uses the dataset\'s real token stats and published provider pricing to estimate cost/time for an edited n_epochs / LoRA vs full setting. Does not create a job.
     * Re-estimate fine-tuning cost and duration
     */
    async finetuningJobsEstimateCreate(requestParameters: FinetuningJobsEstimateCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningEstimateResponse> {
        const response = await this.finetuningJobsEstimateCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * List events for a finetuning job
     */
    async finetuningJobsEventsListRaw(requestParameters: FinetuningJobsEventsListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PaginatedFinetuningJobEventList>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling finetuningJobsEventsList().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['baseModel'] != null) {
            queryParameters['base_model'] = requestParameters['baseModel'];
        }

        if (requestParameters['capability'] != null) {
            queryParameters['capability'] = requestParameters['capability'];
        }

        if (requestParameters['createdAfter'] != null) {
            queryParameters['created_after'] = (requestParameters['createdAfter'] as any).toISOString();
        }

        if (requestParameters['createdBefore'] != null) {
            queryParameters['created_before'] = (requestParameters['createdBefore'] as any).toISOString();
        }

        if (requestParameters['dataset'] != null) {
            queryParameters['dataset'] = requestParameters['dataset'];
        }

        if (requestParameters['groupId'] != null) {
            queryParameters['group_id'] = requestParameters['groupId'];
        }

        if (requestParameters['ordering'] != null) {
            queryParameters['ordering'] = requestParameters['ordering'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['page_size'] = requestParameters['pageSize'];
        }

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/{id}/events/`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PaginatedFinetuningJobEventListFromJSON(jsonValue));
    }

    /**
     * List events for a finetuning job
     */
    async finetuningJobsEventsList(requestParameters: FinetuningJobsEventsListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PaginatedFinetuningJobEventList> {
        const response = await this.finetuningJobsEventsListRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * List finetuning jobs
     */
    async finetuningJobsListRaw(requestParameters: FinetuningJobsListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PaginatedFinetuningJobListList>> {
        const queryParameters: any = {};

        if (requestParameters['baseModel'] != null) {
            queryParameters['base_model'] = requestParameters['baseModel'];
        }

        if (requestParameters['capability'] != null) {
            queryParameters['capability'] = requestParameters['capability'];
        }

        if (requestParameters['createdAfter'] != null) {
            queryParameters['created_after'] = (requestParameters['createdAfter'] as any).toISOString();
        }

        if (requestParameters['createdBefore'] != null) {
            queryParameters['created_before'] = (requestParameters['createdBefore'] as any).toISOString();
        }

        if (requestParameters['dataset'] != null) {
            queryParameters['dataset'] = requestParameters['dataset'];
        }

        if (requestParameters['groupId'] != null) {
            queryParameters['group_id'] = requestParameters['groupId'];
        }

        if (requestParameters['ordering'] != null) {
            queryParameters['ordering'] = requestParameters['ordering'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['page_size'] = requestParameters['pageSize'];
        }

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PaginatedFinetuningJobListListFromJSON(jsonValue));
    }

    /**
     * List finetuning jobs
     */
    async finetuningJobsList(requestParameters: FinetuningJobsListRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PaginatedFinetuningJobListList> {
        const response = await this.finetuningJobsListRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Return step-indexed train/eval loss points for the training monitor.  Preference order: 1. ``job.progress.metrics.loss`` (live poll snapshot) 2. ``job.result.epoch_losses`` (completion snapshot) 3. ``FinetuningJobEvent`` progress events (legacy)
     * Loss / metric curve data for a finetuning job
     */
    async finetuningJobsLossCurvesRetrieveRaw(requestParameters: FinetuningJobsLossCurvesRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<{ [key: string]: any; }>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling finetuningJobsLossCurvesRetrieve().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/{id}/loss-curves/`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse<any>(response);
    }

    /**
     * Return step-indexed train/eval loss points for the training monitor.  Preference order: 1. ``job.progress.metrics.loss`` (live poll snapshot) 2. ``job.result.epoch_losses`` (completion snapshot) 3. ``FinetuningJobEvent`` progress events (legacy)
     * Loss / metric curve data for a finetuning job
     */
    async finetuningJobsLossCurvesRetrieve(requestParameters: FinetuningJobsLossCurvesRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<{ [key: string]: any; }> {
        const response = await this.finetuningJobsLossCurvesRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Dataset-size-aware defaults (batch, epochs, learning rate, LoRA config) plus per-knob provenance for an arbitrary catalog model — the same derivation as the recommendation rows, no LLM call. Serves the wizard\'s \'Add model\' and model-switch flows so the client never fabricates hyperparameter defaults.
     * Recommended hyperparameters for one model
     */
    async finetuningJobsModelDefaultsCreateRaw(requestParameters: FinetuningJobsModelDefaultsCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningExperiment>> {
        if (requestParameters['finetuningModelDefaultsRequestRequest'] == null) {
            throw new runtime.RequiredError(
                'finetuningModelDefaultsRequestRequest',
                'Required parameter "finetuningModelDefaultsRequestRequest" was null or undefined when calling finetuningJobsModelDefaultsCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/model-defaults/`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FinetuningModelDefaultsRequestRequestToJSON(requestParameters['finetuningModelDefaultsRequestRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningExperimentFromJSON(jsonValue));
    }

    /**
     * Dataset-size-aware defaults (batch, epochs, learning rate, LoRA config) plus per-knob provenance for an arbitrary catalog model — the same derivation as the recommendation rows, no LLM call. Serves the wizard\'s \'Add model\' and model-switch flows so the client never fabricates hyperparameter defaults.
     * Recommended hyperparameters for one model
     */
    async finetuningJobsModelDefaultsCreate(requestParameters: FinetuningJobsModelDefaultsCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningExperiment> {
        const response = await this.finetuningJobsModelDefaultsCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Copy-paste prompt that points the capability\'s code at its model alias
     */
    async finetuningJobsModelSwapPromptRetrieveRaw(requestParameters: FinetuningJobsModelSwapPromptRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<ModelSwapPrompt>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling finetuningJobsModelSwapPromptRetrieve().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['pin'] != null) {
            queryParameters['pin'] = requestParameters['pin'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/{id}/model-swap-prompt/`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => ModelSwapPromptFromJSON(jsonValue));
    }

    /**
     * Copy-paste prompt that points the capability\'s code at its model alias
     */
    async finetuningJobsModelSwapPromptRetrieve(requestParameters: FinetuningJobsModelSwapPromptRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<ModelSwapPrompt> {
        const response = await this.finetuningJobsModelSwapPromptRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Return the curated model catalog organised by tier.  When ``FINETUNING_BACKEND`` is ``baseten``, the Baseten catalog from models.json is returned. Pass ``?has_tool_calling=true`` to receive only models that support function-calling fine-tuning. Tiers that contain no eligible models are omitted from the response.
     * List available fine-tuning models by tier
     */
    async finetuningJobsModelsRetrieveRaw(requestParameters: FinetuningJobsModelsRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningModelCatalogResponse>> {
        const queryParameters: any = {};

        if (requestParameters['hasToolCalling'] != null) {
            queryParameters['has_tool_calling'] = requestParameters['hasToolCalling'];
        }

        if (requestParameters['maxContext'] != null) {
            queryParameters['max_context'] = requestParameters['maxContext'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/models/`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningModelCatalogResponseFromJSON(jsonValue));
    }

    /**
     * Return the curated model catalog organised by tier.  When ``FINETUNING_BACKEND`` is ``baseten``, the Baseten catalog from models.json is returned. Pass ``?has_tool_calling=true`` to receive only models that support function-calling fine-tuning. Tiers that contain no eligible models are omitted from the response.
     * List available fine-tuning models by tier
     */
    async finetuningJobsModelsRetrieve(requestParameters: FinetuningJobsModelsRetrieveRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningModelCatalogResponse> {
        const response = await this.finetuningJobsModelsRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Update finetuning job metadata
     */
    async finetuningJobsPartialUpdateRaw(requestParameters: FinetuningJobsPartialUpdateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningJob>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling finetuningJobsPartialUpdate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/{id}/`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'PATCH',
            headers: headerParameters,
            query: queryParameters,
            body: PatchedFinetuningJobRequestToJSON(requestParameters['patchedFinetuningJobRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningJobFromJSON(jsonValue));
    }

    /**
     * Update finetuning job metadata
     */
    async finetuningJobsPartialUpdate(requestParameters: FinetuningJobsPartialUpdateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningJob> {
        const response = await this.finetuningJobsPartialUpdateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Rank the trainable catalog for this dataset, with the benchmark evidence behind each grade and the reason every excluded model was dropped.
     * Rank trainable models for a dataset + capability
     */
    async finetuningJobsRecommendCreateRaw(requestParameters: FinetuningJobsRecommendCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningRecommendationResponse>> {
        if (requestParameters['finetuningRecommendRequestRequest'] == null) {
            throw new runtime.RequiredError(
                'finetuningRecommendRequestRequest',
                'Required parameter "finetuningRecommendRequestRequest" was null or undefined when calling finetuningJobsRecommendCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/recommend/`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: FinetuningRecommendRequestRequestToJSON(requestParameters['finetuningRecommendRequestRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningRecommendationResponseFromJSON(jsonValue));
    }

    /**
     * Rank the trainable catalog for this dataset, with the benchmark evidence behind each grade and the reason every excluded model was dropped.
     * Rank trainable models for a dataset + capability
     */
    async finetuningJobsRecommendCreate(requestParameters: FinetuningJobsRecommendCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningRecommendationResponse> {
        const response = await this.finetuningJobsRecommendCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Get finetuning job (with events)
     */
    async finetuningJobsRetrieveRaw(requestParameters: FinetuningJobsRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningJob>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling finetuningJobsRetrieve().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/{id}/`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningJobFromJSON(jsonValue));
    }

    /**
     * Get finetuning job (with events)
     */
    async finetuningJobsRetrieve(requestParameters: FinetuningJobsRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningJob> {
        const response = await this.finetuningJobsRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Retry a failed finetuning job
     */
    async finetuningJobsRetryCreateRaw(requestParameters: FinetuningJobsRetryCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<FinetuningJob>> {
        if (requestParameters['id'] == null) {
            throw new runtime.RequiredError(
                'id',
                'Required parameter "id" was null or undefined when calling finetuningJobsRetryCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/{id}/retry/`;
        urlPath = urlPath.replace(`{${"id"}}`, encodeURIComponent(String(requestParameters['id'])));

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => FinetuningJobFromJSON(jsonValue));
    }

    /**
     * Retry a failed finetuning job
     */
    async finetuningJobsRetryCreate(requestParameters: FinetuningJobsRetryCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<FinetuningJob> {
        const response = await this.finetuningJobsRetryCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * The training history lists *runs*, not jobs: the wizard launches one job per experiment and stamps them with a shared `group_id`. Paging jobs would straddle a run across a page boundary and silently corrupt every per-run aggregate the client derives (status, progress, cost, duration, and the single-job test that picks the row\'s navigation target), so this pages over distinct runs and returns each one whole.  `count` is the number of matching **runs**. `results` carries one entry per run — `{run_id, group_id, jobs}` — where `run_id` is the `group_id` when there is one and the lone job\'s id otherwise, and `jobs` is every job of that run so client-side aggregation is always complete. Runs are ordered by their most recent job; `?ordering=` is accepted for compatibility but does not reorder runs.  Every filter of the jobs list applies (`project`, `capability`, `dataset`, `base_model`, `status`, `provider`, `group_id`, `search`).
     * List training runs — whole runs per page, newest first
     */
    async finetuningJobsRunsListRaw(requestParameters: FinetuningJobsRunsListRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<PaginatedFinetuningJobRunList>> {
        const queryParameters: any = {};

        if (requestParameters['baseModel'] != null) {
            queryParameters['base_model'] = requestParameters['baseModel'];
        }

        if (requestParameters['capability'] != null) {
            queryParameters['capability'] = requestParameters['capability'];
        }

        if (requestParameters['createdAfter'] != null) {
            queryParameters['created_after'] = (requestParameters['createdAfter'] as any).toISOString();
        }

        if (requestParameters['createdBefore'] != null) {
            queryParameters['created_before'] = (requestParameters['createdBefore'] as any).toISOString();
        }

        if (requestParameters['dataset'] != null) {
            queryParameters['dataset'] = requestParameters['dataset'];
        }

        if (requestParameters['groupId'] != null) {
            queryParameters['group_id'] = requestParameters['groupId'];
        }

        if (requestParameters['ordering'] != null) {
            queryParameters['ordering'] = requestParameters['ordering'];
        }

        if (requestParameters['page'] != null) {
            queryParameters['page'] = requestParameters['page'];
        }

        if (requestParameters['pageSize'] != null) {
            queryParameters['page_size'] = requestParameters['pageSize'];
        }

        if (requestParameters['project'] != null) {
            queryParameters['project'] = requestParameters['project'];
        }

        if (requestParameters['provider'] != null) {
            queryParameters['provider'] = requestParameters['provider'];
        }

        if (requestParameters['search'] != null) {
            queryParameters['search'] = requestParameters['search'];
        }

        if (requestParameters['status'] != null) {
            queryParameters['status'] = requestParameters['status'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/runs/`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => PaginatedFinetuningJobRunListFromJSON(jsonValue));
    }

    /**
     * The training history lists *runs*, not jobs: the wizard launches one job per experiment and stamps them with a shared `group_id`. Paging jobs would straddle a run across a page boundary and silently corrupt every per-run aggregate the client derives (status, progress, cost, duration, and the single-job test that picks the row\'s navigation target), so this pages over distinct runs and returns each one whole.  `count` is the number of matching **runs**. `results` carries one entry per run — `{run_id, group_id, jobs}` — where `run_id` is the `group_id` when there is one and the lone job\'s id otherwise, and `jobs` is every job of that run so client-side aggregation is always complete. Runs are ordered by their most recent job; `?ordering=` is accepted for compatibility but does not reorder runs.  Every filter of the jobs list applies (`project`, `capability`, `dataset`, `base_model`, `status`, `provider`, `group_id`, `search`).
     * List training runs — whole runs per page, newest first
     */
    async finetuningJobsRunsList(requestParameters: FinetuningJobsRunsListRequest = {}, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<PaginatedFinetuningJobRunList> {
        const response = await this.finetuningJobsRunsListRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Check whether a dataset\'s datapoints conform to Together AI\'s format.
     * Validate a dataset for fine-tuning
     */
    async finetuningJobsValidateDatasetCreateRaw(requestParameters: FinetuningJobsValidateDatasetCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<DatasetValidationResponse>> {
        if (requestParameters['datasetValidateRequestRequest'] == null) {
            throw new runtime.RequiredError(
                'datasetValidateRequestRequest',
                'Required parameter "datasetValidateRequestRequest" was null or undefined when calling finetuningJobsValidateDatasetCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/finetuning-jobs/validate-dataset/`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: DatasetValidateRequestRequestToJSON(requestParameters['datasetValidateRequestRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => DatasetValidationResponseFromJSON(jsonValue));
    }

    /**
     * Check whether a dataset\'s datapoints conform to Together AI\'s format.
     * Validate a dataset for fine-tuning
     */
    async finetuningJobsValidateDatasetCreate(requestParameters: FinetuningJobsValidateDatasetCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<DatasetValidationResponse> {
        const response = await this.finetuningJobsValidateDatasetCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

}

/**
 * @export
 */
export const FinetuningJobsEventsListProviderEnum = {
    baseten: 'baseten',
    modal: 'modal',
    nebius: 'nebius',
    tinker: 'tinker',
    together_ai: 'together_ai'
} as const;
export type FinetuningJobsEventsListProviderEnum = typeof FinetuningJobsEventsListProviderEnum[keyof typeof FinetuningJobsEventsListProviderEnum];
/**
 * @export
 */
export const FinetuningJobsEventsListStatusEnum = {
    cancelled: 'cancelled',
    deploying: 'deploying',
    failed: 'failed',
    preparing: 'preparing',
    queued: 'queued',
    running: 'running',
    succeeded: 'succeeded'
} as const;
export type FinetuningJobsEventsListStatusEnum = typeof FinetuningJobsEventsListStatusEnum[keyof typeof FinetuningJobsEventsListStatusEnum];
/**
 * @export
 */
export const FinetuningJobsListProviderEnum = {
    baseten: 'baseten',
    modal: 'modal',
    nebius: 'nebius',
    tinker: 'tinker',
    together_ai: 'together_ai'
} as const;
export type FinetuningJobsListProviderEnum = typeof FinetuningJobsListProviderEnum[keyof typeof FinetuningJobsListProviderEnum];
/**
 * @export
 */
export const FinetuningJobsListStatusEnum = {
    cancelled: 'cancelled',
    deploying: 'deploying',
    failed: 'failed',
    preparing: 'preparing',
    queued: 'queued',
    running: 'running',
    succeeded: 'succeeded'
} as const;
export type FinetuningJobsListStatusEnum = typeof FinetuningJobsListStatusEnum[keyof typeof FinetuningJobsListStatusEnum];
/**
 * @export
 */
export const FinetuningJobsRunsListProviderEnum = {
    baseten: 'baseten',
    modal: 'modal',
    nebius: 'nebius',
    tinker: 'tinker',
    together_ai: 'together_ai'
} as const;
export type FinetuningJobsRunsListProviderEnum = typeof FinetuningJobsRunsListProviderEnum[keyof typeof FinetuningJobsRunsListProviderEnum];
/**
 * @export
 */
export const FinetuningJobsRunsListStatusEnum = {
    cancelled: 'cancelled',
    deploying: 'deploying',
    failed: 'failed',
    preparing: 'preparing',
    queued: 'queued',
    running: 'running',
    succeeded: 'succeeded'
} as const;
export type FinetuningJobsRunsListStatusEnum = typeof FinetuningJobsRunsListStatusEnum[keyof typeof FinetuningJobsRunsListStatusEnum];
