/* tslint:disable */
/* eslint-disable */
/**
 * Overbae API
 * Backend API for the Overclaw / Overmind platform.
 *
 * The version of the OpenAPI document: 2.0.0
 *
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */


import * as runtime from '../runtime';
import type {
  SyncSnapshot,
  SyncSnapshotRequest,
} from '../models/index';
import {
    SyncSnapshotFromJSON,
    SyncSnapshotToJSON,
    SyncSnapshotRequestFromJSON,
    SyncSnapshotRequestToJSON,
} from '../models/index';

export interface V1ModelsDestroyRequest {
    modelId: string;
}

export interface V1ModelsRetrieve2Request {
    modelId: string;
}

export interface V1SyncCreateRequest {
    syncSnapshotRequest: SyncSnapshotRequest;
}

export interface V1SyncRetrieveRequest {
    projectId: string;
}

/**
 *
 */
export class V1Api extends runtime.BaseAPI {

    /**
     */
    async v1ChatCompletionsCreateRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/chat/completions`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     */
    async v1ChatCompletionsCreate(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.v1ChatCompletionsCreateRaw(initOverrides);
    }

    /**
     */
    async v1ModelsDestroyRaw(requestParameters: V1ModelsDestroyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['modelId'] == null) {
            throw new runtime.RequiredError(
                'modelId',
                'Required parameter "modelId" was null or undefined when calling v1ModelsDestroy().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/models/{model_id}`;
        urlPath = urlPath.replace(`{${"model_id"}}`, encodeURIComponent(String(requestParameters['modelId'])));

        const response = await this.request({
            path: urlPath,
            method: 'DELETE',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     */
    async v1ModelsDestroy(requestParameters: V1ModelsDestroyRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.v1ModelsDestroyRaw(requestParameters, initOverrides);
    }

    /**
     */
    async v1ModelsRetrieveRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/models`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     */
    async v1ModelsRetrieve(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.v1ModelsRetrieveRaw(initOverrides);
    }

    /**
     */
    async v1ModelsRetrieve2Raw(requestParameters: V1ModelsRetrieve2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        if (requestParameters['modelId'] == null) {
            throw new runtime.RequiredError(
                'modelId',
                'Required parameter "modelId" was null or undefined when calling v1ModelsRetrieve2().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/models/{model_id}`;
        urlPath = urlPath.replace(`{${"model_id"}}`, encodeURIComponent(String(requestParameters['modelId'])));

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     */
    async v1ModelsRetrieve2(requestParameters: V1ModelsRetrieve2Request, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.v1ModelsRetrieve2Raw(requestParameters, initOverrides);
    }

    /**
     * Two-way ``overmind.toml`` sync.  POST pushes the local snapshot. The server reconciles capability identity — carry / leftover / remount — and never deletes: absence sets ``status=leftover`` (observed rows stay). ``archived=true`` keeps leftover. POST response is the incoming set with ids filled in. GET returns leftovers with ``archived=true`` so toml round-trips them without listing them in the console.  Auth: ``X-Api-Key`` (``APIToken``). Project keys must match ``project_id``; account keys may sync any project the user belongs to.
     */
    async v1SyncCreateRaw(requestParameters: V1SyncCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncSnapshot>> {
        if (requestParameters['syncSnapshotRequest'] == null) {
            throw new runtime.RequiredError(
                'syncSnapshotRequest',
                'Required parameter "syncSnapshotRequest" was null or undefined when calling v1SyncCreate().'
            );
        }

        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        headerParameters['Content-Type'] = 'application/json';

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/sync`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
            body: SyncSnapshotRequestToJSON(requestParameters['syncSnapshotRequest']),
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncSnapshotFromJSON(jsonValue));
    }

    /**
     * Two-way ``overmind.toml`` sync.  POST pushes the local snapshot. The server reconciles capability identity — carry / leftover / remount — and never deletes: absence sets ``status=leftover`` (observed rows stay). ``archived=true`` keeps leftover. POST response is the incoming set with ids filled in. GET returns leftovers with ``archived=true`` so toml round-trips them without listing them in the console.  Auth: ``X-Api-Key`` (``APIToken``). Project keys must match ``project_id``; account keys may sync any project the user belongs to.
     */
    async v1SyncCreate(requestParameters: V1SyncCreateRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncSnapshot> {
        const response = await this.v1SyncCreateRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     * Two-way ``overmind.toml`` sync.  POST pushes the local snapshot. The server reconciles capability identity — carry / leftover / remount — and never deletes: absence sets ``status=leftover`` (observed rows stay). ``archived=true`` keeps leftover. POST response is the incoming set with ids filled in. GET returns leftovers with ``archived=true`` so toml round-trips them without listing them in the console.  Auth: ``X-Api-Key`` (``APIToken``). Project keys must match ``project_id``; account keys may sync any project the user belongs to.
     */
    async v1SyncRetrieveRaw(requestParameters: V1SyncRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<SyncSnapshot>> {
        if (requestParameters['projectId'] == null) {
            throw new runtime.RequiredError(
                'projectId',
                'Required parameter "projectId" was null or undefined when calling v1SyncRetrieve().'
            );
        }

        const queryParameters: any = {};

        if (requestParameters['projectId'] != null) {
            queryParameters['project_id'] = requestParameters['projectId'];
        }

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/sync`;

        const response = await this.request({
            path: urlPath,
            method: 'GET',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.JSONApiResponse(response, (jsonValue) => SyncSnapshotFromJSON(jsonValue));
    }

    /**
     * Two-way ``overmind.toml`` sync.  POST pushes the local snapshot. The server reconciles capability identity — carry / leftover / remount — and never deletes: absence sets ``status=leftover`` (observed rows stay). ``archived=true`` keeps leftover. POST response is the incoming set with ids filled in. GET returns leftovers with ``archived=true`` so toml round-trips them without listing them in the console.  Auth: ``X-Api-Key`` (``APIToken``). Project keys must match ``project_id``; account keys may sync any project the user belongs to.
     */
    async v1SyncRetrieve(requestParameters: V1SyncRetrieveRequest, initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<SyncSnapshot> {
        const response = await this.v1SyncRetrieveRaw(requestParameters, initOverrides);
        return await response.value();
    }

    /**
     */
    async v1TracesCreateRaw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/api/v1/traces`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     */
    async v1TracesCreate(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.v1TracesCreateRaw(initOverrides);
    }

    /**
     */
    async v1TracesCreate2Raw(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<runtime.ApiResponse<void>> {
        const queryParameters: any = {};

        const headerParameters: runtime.HTTPHeaders = {};

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("ClerkBearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }
        if (this.configuration && this.configuration.apiKey) {
            headerParameters["X-Api-Key"] = await this.configuration.apiKey("X-Api-Key"); // ApiKeyAuth authentication
        }

        if (this.configuration && this.configuration.accessToken) {
            const token = this.configuration.accessToken;
            const tokenString = await token("BearerAuth", []);

            if (tokenString) {
                headerParameters["Authorization"] = `Bearer ${tokenString}`;
            }
        }

        let urlPath = `/v1/traces`;

        const response = await this.request({
            path: urlPath,
            method: 'POST',
            headers: headerParameters,
            query: queryParameters,
        }, initOverrides);

        return new runtime.VoidApiResponse(response);
    }

    /**
     */
    async v1TracesCreate2(initOverrides?: RequestInit | runtime.InitOverrideFunction): Promise<void> {
        await this.v1TracesCreate2Raw(initOverrides);
    }

}
