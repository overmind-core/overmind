"""Benchmark-grounded model grading: taxonomy, artifact schema, and the grading maths.

Pure logic — no network, no ORM. The committed artifact under ``data/`` is the only
input; ``sync/`` regenerates it out of band.
"""
